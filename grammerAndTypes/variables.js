/*
Variables:
-Are named containers for storing data values.
-We name the variables so that we can refer to them and the value they hold at a later time.
*/

let a = 2;

/*
(1)         (2)     (3)      (4)
let          b       =        2

1. JavaScript keyword to denote the creation of a variable
2. The name of the variable, in which the developer refers to in order to grab the value stored
3. The operator we use to assign values
4. The inital value
*/ 

// The declaration is to the left of the assignment operator(=)
// This is where the variable is created, by utilizing a designed JavaScript keyword(let, var, const) and giving it a name.

let s;

// The intialization is to the right side of the assignment operator(=)
//This is where the developer sets the value for the declared variable
//      (1)
let x = 10;

// 1. is the Initalization

let y;
console.log("Declaration:",y)

y = 10;
console.log("Initialization", y);

y = 35;
console.log("Initialization 2:", y);